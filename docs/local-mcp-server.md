# Local MCP Server

Local MCP server that exposes `@utaba/deep-memory` as Model Context Protocol tools for AI agents (Claude Code, Claude Desktop, etc.).

**Full documentation:** [`packages/mcp-server/README.md`](../packages/mcp-server/README.md) — installation, Claude Code wiring, the 28 tools, configuration, and export/import APIs.

> Indexing pipeline tools live in a separate server — [`@utaba/deep-memory-indexer-mcp-server`](../packages/indexer-mcp-server/README.md).
